{
  "name": "medkonspekt-static",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "test": "npm run build && node --test tests/*.test.mjs"
  }
}
